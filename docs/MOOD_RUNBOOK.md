# MOOD Runbook

1. Run `make test`.
2. Run `make verify`.
3. Run `make docker-build`.
4. Inspect the `Full Stack MOOD` CI run.
5. Capture the live verification log as screenshot evidence only after all checks are green.

Failure policy: a failed verification or MOOD dissent routes to investigation; it is never relabeled as success.
