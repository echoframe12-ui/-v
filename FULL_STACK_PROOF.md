# Full Stack Proof

The full-stack MOOD build is considered proven only when CI executes:

- `make test`
- `make verify`
- `make docker-build`

The live verification log is the screenshot evidence source.
